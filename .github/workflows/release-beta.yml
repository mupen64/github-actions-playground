name: Beta Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number (e.g. 1.2.0-beta)"
        required: true
        type: string

jobs:
  beta-release:
    name: Create Beta Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Get latest commit hash
        id: commit
        run: echo "short_hash=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Download mupen-release artifact from latest main run
        uses: dawidd6/action-download-artifact@v6
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          workflow: upload-dummy-artifact.yml
          branch: main
          name: mupen-release
          path: ./release-artifact

      - name: Install git-cliff
        uses: taiki-e/install-action@v2
        with:
          tool: git-cliff

      - name: Generate changelog with git-cliff
        id: changelog
        run: |
          git cliff --latest --output CHANGELOG_DRAFT.md || git cliff --output CHANGELOG_DRAFT.md
          {
            echo 'body<<EOF'
            cat CHANGELOG_DRAFT.md
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Print release details to summary
        run: |
          echo "## 🚀 Draft Beta Release Created" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Field   | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| **Tag** | \`${{ inputs.version }}-${{ steps.commit.outputs.short_hash }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### 📋 Generated Changelog" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          cat CHANGELOG_DRAFT.md >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "> ✏️ **Next step:** Go to [GitHub Releases](../../releases) to review, edit, and publish the draft." >> "$GITHUB_STEP_SUMMARY"

      - name: Create draft beta release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}-${{ steps.commit.outputs.short_hash }}
          name: ${{ inputs.version }}-${{ steps.commit.outputs.short_hash }}
          body: ${{ steps.changelog.outputs.body }}
          prerelease: true
          draft: true
          files: ./release-artifact/**
